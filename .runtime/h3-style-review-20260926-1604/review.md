# H3 画风核验 · 2026-09-26

当前结论：最新正式视频仍执行旧版拆图流程；它不能代表完整四视图方案的成片效果。第一版完整人物板短样片已完成，但画风及人物身份仍未达标：从夸张卡通变为偏真人的表现，不能算修复成功。

## 已核实的正式视频

- 数据库视频：378；视频段：1790319537445；语言：en-US。
- 成片：`data/oss/1790218938911/video/d177d497-0962-425c-9046-baf28382129f.mp4`。
- ComfyUI 任务：`30fddc94-8d90-45ee-bb83-62b47c6ba442`。
- 实际参考：Ava 脸部/正面、Madison 脸部/正面、场景、鲨鱼，共 6 张。
- 取帧：`frame-1.png`、`frame-4.8.png`、`frame-7.png`、`frame-10.png`。
- 实际运行后端 PID 29296 于 10:26 启动，直接 tsx，无自动重载。之后的源码和构建修改尚未载入此进程。它再次生成并保存了旧版 6 图英文提示词。

## 当前修复与验证

- 维持每个人物只使用一张完整四视图板的提交规则。
- 视频风格规则要求继承当前人物图的眼脸比例、鼻颌结构和可见材质表现；不增加引擎或画风标签。
- 页面在单段/批量请求发出前检查每种所选语言的 Picture 编号与实际独立图片数。
- 没有对白的片段不再被语义审查要求给风浪声附加 en-US 标签。
- 发现原有审核没有收到生成器使用的视觉手册。现已把相同的实际手册传给既有基版/翻译审核，检查明确要求的画风来源继承，不增加审核轮次。
- 第二轮正式剧情的真实生成与翻译已通过审核，输出见 `formal-prompt-v2/1790319537445/base.txt` 和 `en-US.txt`；风格开头明确继承参考图的写实程度、面部比例和可见材质。此处仍仅为提示词验收。
- H3、翻译、参考计划、提交边界和页面检查相关 146 项测试通过；前后端构建通过。
- 本机 10588 返回的网页与新构建文件哈希一致，包含新增页面检查。已打开的页面仍需刷新；这不代表旧后端模块已更新。

## 实际短样片

- 请求时长 4 秒；模型帧数约束下 107 帧 / 24 fps。
- 使用程序生成并审核的提示词，固定近景、两张脸可见、轻微视线/头发运动，以便观察身份和材质。
- 输入：Ava 完整人物板、Madison 完整人物板、原场景、原鲨鱼，共 4 张；原文件尺寸与哈希记录在 `style-probe-manifest.json`。
- 实际提交任务：`672bcb53-1c1d-4ed0-985c-db61409bfc99`，提交回执 `style-probe-receipt.json`。
- 使用当前原生 H3 工作流、Ref2VA INT8、Qwen AWQ、20 步、1344×768；无 Turbo/LoRA/放大节点。
- 固定种子仅用于复现本次样片，未证明与视频 378 的采样种子相同。镜头内容也不同，因此这不是单变量 A/B 实验。
- `ref_image_size=match` 会将 5008×1888 整板缩放至 1664×608。每张板仍只占一个槽位，四个面板完整保留；不能据此预先保证细节保真。
- 不修改正式剧情、现有视频历史或队列顺序。样片单独保存，不覆盖正式视频。

## 验收边界

第一版样片于 16:36 输出：`style-probe-output.mp4`，实际 4.458333 秒，1344×768、24 fps、H264+AAC。SHA256：`867ec7255fa3ef2627a3baf52f3ce448cc259cb6c4393a4813388ac645cea852`。

取帧 `probe-frame-0.3.png` 与 `probe-frame-3.0.png` 的独立复核结论：Ava 的口鼻/下颌轮廓与额前头发改变；Madison 的卷发长度和蓬松程度、口鼻比例改变；皮肤与粉色睡衣的表面表现也偏离原角色板。两帧之间大体稳定，主要问题是生成起点就偏离参考，不能据此称为持续逐帧变脸。

第二版受控短测于 16:38 提交、17:05 完成：`433529ff-3149-4fe9-9b9c-1a17636bbbdb`，证据目录 `../h3-style-probe-v2-20260926-1638/`。仅替换风格开头，采用上述真实程序产出的来源继承描述；除输出文件名外，工作流其余设置、四张上传图片、种子、镜头、音效与帧数均逐项比较相同。输出 SHA256：`29f1fe6464c9c71a937238789e013198cf258296d6b9ddab353d03300eb12b9d`。对照 0.3 秒和 3.0 秒画面，仍偏真人，脸和发型未回到人物参考，判定未通过。本次单一对照只支持“这两句来源继承表述不足以解决该样例”，不能推广成任何提示词都无效。

第三版于 17:06 提交：`11468e1a-2804-46b2-b75b-fd9fbb83fee8`，证据目录 `../h3-style-probe-v3-20260926-1652/`。保留第二版提示词、种子、四张完整图片、镜头与参数；仅将人物整板的模型有效尺寸从 1664×608 提高到 2304×864，通过 Lanczos 预缩放和 H3 的 max 参考尺寸选项实现。场景和鲨鱼仍为 1344×768 / 1024×1024，已按本机 Comfy 源码复现并验证缩放后像素哈希一致。图像参考部分 DiT rows 增加约 47.7%，不代表显存或耗时同比变化。结果待核验，生产默认参数未改。

规则和测试通过只说明提示词与提交链路满足已检查条件，不能证明成片画风匹配。须检查样片实物后补充结果；短近景的成功也不能代替正式 11 秒多镜头成片验证。

运行中的旧批次仍依赖原后端进程，本次未重启或取消它们。正式页面后续生成还需要后端加载新代码，不能把前端构建完成当作运行链路已全部更新。
